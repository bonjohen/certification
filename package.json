{
  "name": "certification-quiz",
  "version": "1.0.0",
  "description": "Certification exam quiz application",
  "type": "module",
  "scripts": {
    "start": "npx http-server . -p 8090 -c-1 -o",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "axe-core": "^4.11.2",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
